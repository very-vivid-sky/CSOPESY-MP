#pragma once
#include <iostream> 
#include <mutex>
#include <vector>


typedef std::string String;
typedef std::time_t Time_t;
typedef std::mutex Mutex;
typedef std::mutex Muted;
typedef std::thread Thread;
typedef const char* constCharString;